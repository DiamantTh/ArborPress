# ArborPress Apache-Hinweise
#
# Nur die relevanten Sicherheitsblöcke, keine vollständigen VirtualHosts.
# Module: proxy proxy_http ssl headers rewrite
# Optional: remoteip

<VirtualHost *:80>
    ServerName blog.example.com

    Alias /.well-known/acme-challenge/ /var/www/certbot/.well-known/acme-challenge/
    <Directory /var/www/certbot/.well-known/acme-challenge/>
        Options None
        AllowOverride None
        Require all granted
    </Directory>

    RewriteEngine On
    RewriteCond %{REQUEST_URI} !^/\.well-known/acme-challenge
    RewriteRule ^ https://%{SERVER_NAME}%{REQUEST_URI} [END,NE,R=permanent]
</VirtualHost>

<VirtualHost *:443>
    ServerName blog.example.com

    SSLEngine on
    SSLCertificateFile    /etc/letsencrypt/live/blog.example.com/fullchain.pem
    SSLCertificateKeyFile /etc/letsencrypt/live/blog.example.com/privkey.pem

    SSLProtocol         all -SSLv3 -TLSv1 -TLSv1.1
    SSLCipherSuite      ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305
    SSLHonorCipherOrder off
    SSLSessionTickets   off

    ProxyPreserveHost On
    ProxyPass        /static/ !
    ProxyPass        / http://127.0.0.1:8066/
    ProxyPassReverse / http://127.0.0.1:8066/
    ProxyTimeout 60

    RequestHeader set    X-Forwarded-Proto "https"
    RequestHeader append X-Forwarded-For   "%{REMOTE_ADDR}e"

    Alias /static/ /path/to/arborpress/static/
    <Directory /path/to/arborpress/static/>
        Options -Indexes
        AllowOverride None
        Require all granted
        <IfModule mod_expires.c>
            ExpiresActive On
            ExpiresDefault "access plus 1 year"
        </IfModule>
        Header always set Cache-Control "public, max-age=31536000, immutable"
        Header always set X-Content-Type-Options "nosniff"
    </Directory>

    LimitRequestBody 52428800
</VirtualHost>
